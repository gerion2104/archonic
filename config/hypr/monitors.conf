# Monitorspezifisch -- der einzige Teil, der sich pro Rechner unterscheidet.
# Bewusst NICHT im Repo pflegen, sondern per Hostname laden:
#   source = ~/.config/hypr/monitors.$(hostname).conf
monitor = ,preferred,auto,1
